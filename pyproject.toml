[project]
name = "image-processor"
version = "0.2.0"
description = "Create youtube cover"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.1.1",
    "psd-tools>=1.14.1",
    "psycopg2-binary>=2.9.11",
]

[dependency-groups]
dev = [
    "ipython>=9.11.0",
    "ruff>=0.15.6",
]
